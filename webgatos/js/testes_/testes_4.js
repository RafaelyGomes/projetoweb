let limite = 100;
let saldo_atual = 10;

let resultado = ( saldo_atual < limite) ? "necessito depositar $$$" : "Não necessito deposito";

alert(resultado)

/*

 Sintaxe:
let variavel = (condição) ? //instrução se true : //instrução se false

/*